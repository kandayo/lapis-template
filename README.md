## Usage

```console
dip provision     # Provision the project (install lapis and project dependencies)
dip tup monitor   # Start tup monitor
dip lapis server  # Start server
dip spec          # Run specs
```
